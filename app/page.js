import VisitorLayout from "./(visitor)/layout";
import LandingPage from "./(visitor)/page";

export default function Home() {
  return (
    <VisitorLayout>
      <LandingPage />
    </VisitorLayout>
  );
}
